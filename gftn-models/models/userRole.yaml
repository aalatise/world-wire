title: UserRole
type: object
required:
  - role
properties:
  userId:
    type: string
    x-go-custom-tag: 'validate:"omitempty"'
  email:
    type: string
    x-go-custom-tag: 'validate:"omitempty"'
  role:
    type: string
    x-go-custom-tag: 'validate:"required"'
externalDocs:
  url: '#/definitions/whitelistRequest'
x-go-name: UserRole